# Findings 2–7 implementation plan

Goal: Keep Telegram publicly usable and allow every authenticated Firebase user to use shared website cases, reports, and review actions. Reserve operational settings for administrators. Never change finding 1 or files under backend/core; preserve all backend folders.

The user authorized execution and chose shared access for all signed-in website users. Changes belong at API/worker boundaries, infrastructure, and frontend presentation. Existing classification, extraction, ingestion, repository, and processing implementations remain unchanged.

- [ ] 2. Restrict Telegram weekly-report disclosure to the configured administrative chat. Give other users a sign-in website link without reading or exposing report data. Validate ISO weeks. Route new publications through an integration adapter using the existing blob store and authenticated previews, avoiding public Drive grants. Keep aggregation and narrative generation unchanged. Cover anonymous, ordinary, admin, missing admin configuration, malformed week, and stored-report cases.
- [ ] 3. Authorize notification reads and mutations before repository access. Keep public bot intake/help/assistance working. Validate arguments without accidental state changes. Restrict website settings and Gmail connection administration using an explicitly provisioned Firebase admin claim; personal theme settings remain available to everyone.
- [ ] 4. Replace the API's reviewer-allowlist dependency with Firebase authentication for shared operations. Never invoke the repository's auto-enrollment method. Require a real UID and valid project token; do not persist a reviewer on login. Add a session capabilities endpoint. Admin claims must be boolean true. Supply an explicit admin provisioning utility, preserving unrelated claims. Test multiple first users, invalid tokens, ordinary shared access, and denied settings access.
- [ ] 5. Use a canonical command catalog for deployment, API help, and operations parsing. Register email_limit; accept the historical email-limit alias. Support Telegram bot suffixes, reject invalid arguments and prefixes, retain the existing ask implementation unchanged. Verify all registered commands have a dispatch path and deployment uses the same catalog.
- [ ] 6. Keep Cloud Run IAM private and add worker boundary verification of Google OIDC audience, issuer, verified service-account email, and route-specific invoker identity. Production misconfiguration fails closed. Configure both new and existing Pub/Sub/Scheduler callers consistently; retain local/test behavior. Test rejected requests have no processing side effects.
- [ ] 7. Default inbox excludes application-classified spam. Provide explicit Spam and All including spam views; reset restores the non-spam default. Preserve search, date/status filters, counts, and access to spam for correction. Test filtering and run frontend checks.

Verification: Run focused regression tests first, then the relevant backend suite, frontend tests/typecheck/lint/build, shell syntax checks, and independent diff review. Compare core file checksums and the finding-1 handler blocks against the initial snapshot. Report any unverified deployment or historical Drive-permission migration separately; source changes do not prove live rollout.

No claim of zero bugs is made: completion requires observed behavior and explicit coverage of the above cases.
